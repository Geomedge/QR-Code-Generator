<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="QR_Code_Generator.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:QR_Code_Generator"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="QR Code Generator">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <Border Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"  HorizontalAlignment="Stretch" VerticalAlignment="Top" Grid.Row="0">
            <TextBlock Margin="15,10,0,2" Text="QR Code Generator" Style="{StaticResource TitleTextBlockStyle}"/>
        </Border>
        <ScrollViewer Grid.Row="1">
            <Frame x:Name="WifiFrame" />
        </ScrollViewer>
    </Grid>
</Window>
