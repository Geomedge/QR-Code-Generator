<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="QR_Code_Generator.MassQRCode"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:QR_Code_Generator"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    <!-- Left Top Right Bottom-->

    <Grid>
        <StackPanel Margin="36,0,36,0">
            <TextBlock Margin="0,40,0,36" Style="{StaticResource TitleTextBlockStyle}" Grid.Row="0">Mass QR Code Generator</TextBlock>

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Button Margin="0,0,0,5" Width="110" Content="Import List" Grid.Column="0" Name="ImportButton" IsEnabled="True"/>
                <Button Margin="5,0,0,5" Width="170" Content="Download Template" Grid.Column="1" Name="DownloadButton" IsEnabled="True"/>
            </Grid>

            <!-- Data List -->
            <ListView x:Name="MyListView" ItemsSource="{Binding ListQR}">


                <ListView.ItemTemplate>
                    <DataTemplate>
                        <Grid Margin="5">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <TextBlock Text="{Binding QRCodeData}" Grid.Column="0" FontSize="16"/>
                            <TextBlock Text="{Binding QRCodeType}" Grid.Column="1" FontSize="16"/>
                            <TextBlock Text="{Binding QRCodeData1}" Grid.Column="2" FontSize="16"/>
                            <TextBlock Text="{Binding QRCodeData2}" Grid.Column="3" FontSize="16"/>
                        </Grid>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </StackPanel>
        <Button VerticalAlignment="Bottom" HorizontalAlignment="Right" Content="Generate Mass QR Codes" Margin="36,20,36,50" Width="200"/>
    </Grid>
</Page>